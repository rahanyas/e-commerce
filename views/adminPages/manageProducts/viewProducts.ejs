<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/viewProducts.css">
  <title>view Products</title>
</head>
<body>
  <!-- Navigation Bar -->
   <nav class="navbar navbar-expand-lg navbar-light bg-light p-3">
    <div class="container-fluid">
      <h1 class="navbar-brand">Products</h1>
      <div class="navbar-nav">
        <a class="nav-link" href="/adminHome">Home</a>
      </div>
    </div>
  </nav>


  <div class="container my-4">
    <a href="" class="btn btn-primary me-2">Search</a>
    <a href="/add-product" class="btn btn-success me-2">Add Products</a>
  </div> 
 


<table class="container table table-striped table-bordered">
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Price</th>
      <th>Image</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% products.forEach(product => { %>
      <tr>
        <td><%= product.name %></td>
        <td><%= product.description %></td>
        <td>$<%= product.price %></td>
        <td>
          <img src="<%= product.images[0]?.url %>" alt="<%= product.name %>" class="img-fluid" style="max-width: 150px;" />
        </td>
        <td>
          <a href="/delete-product/<%= product.id %>" class="btn btn-danger m-2">Delete</a>
          <a href="/edit-product/<%= product.id %>" class="btn btn-success">Edit</a>
        </td>
      </tr>
    <% }); %>
  </tbody>
</table>

  <!-- <nav aria-label="Page navigation">
  <ul class="d-flex justify-content-center  pagination">
    <% for(let i = 1; i <= totalPages; i++) { %>
      <li class="page-item <%= currentPage === i ? 'active' : '' %>">
        <a class="page-link" href="/admin/products?page=<%= i %>&limit=<%= limit %>"><%= i %></a>
      </li>
    <% } %>
  </ul>
</nav> -->

<div class="pagination-controls d-flex justify-content-center mt-4">
  <% if (currentPage > 1) { %>
    <a href="?page=<%= currentPage - 1 %>&limit=<%= limit %>" class="btn btn-primary mx-1">Previous</a>
  <% } %>

  <% for (let i = 1; i <= totalPages; i++) { %>
    <a href="?page=<%= i %>&limit=<%= limit %>"
       class="btn <%= currentPage === i ? 'btn-success' : 'btn-secondary' %> mx-1">
      <%= i %>
    </a>
  <% } %>

  <% if (currentPage < totalPages) { %>
    <a href="?page=<%= currentPage + 1 %>&limit=<%= limit %>" class="btn btn-primary mx-1">Next</a>
  <% } %>
</div>




  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
